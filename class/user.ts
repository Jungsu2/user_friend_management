class User {
    public user_id: string;
    public user_name: string;

    constructor(user_id: string, user_name: string) {
        this.user_id = user_id;
        this.user_name = user_name;
    }

    public static async init(user_id)

}